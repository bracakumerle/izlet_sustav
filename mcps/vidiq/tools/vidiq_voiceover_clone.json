{
  "name": "vidiq_voiceover_clone",
  "description": "Clone a voice from an audio sample and save it to the user's voice library.\n\nProvide a base64-encoded audio sample (a 60-600s MP3/WAV/M4A clip of a single\nspeaker who has consented to being cloned) and a display name. Returns the new\nvoiceId, which can be used with vidiq_voiceover_generate. Each user may keep at\nmost 3 custom voices. Video samples are not accepted — extract the audio first.\n\nCost: 50 credit(s) per call.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "minLength": 1,
        "description": "Display name for the cloned voice (e.g. 'My Voice')."
      },
      "audioBase64": {
        "type": "string",
        "minLength": 1,
        "description": "Base64-encoded audio sample of a single consenting speaker. Must be a 60-600s MP3, WAV, or M4A clip. Video is not accepted — extract the audio first."
      },
      "contentType": {
        "type": "string",
        "description": "MIME type of the sample (e.g. 'audio/mpeg', 'audio/wav'). Defaults to audio/mpeg."
      },
      "description": {
        "type": "string",
        "description": "Optional description of the voice."
      }
    },
    "required": [
      "name",
      "audioBase64"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}