{
  "name": "vidiq_channel_stats",
  "description": "Get YouTube channel statistics including subscriber count, total views, video count, and growth over a configurable time period.\n\nUse this tool to:\n- Look up any YouTube channel's current stats (subscribers, views, videos)\n- See how much a channel has grown over a given period\n- Get channel metadata (country, language, topics, creation date)\n\nCost: 5 credit(s) per call.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "channelId": {
        "type": "string",
        "minLength": 1,
        "description": "YouTube channel ID (e.g. 'UCX6OQ3DkcsbYNE6H8uQQuVA') or handle (e.g. '@MrBeast')"
      },
      "from": {
        "type": "string",
        "format": "date",
        "description": "Start date (YYYY-MM-DD). Defaults to 30 days ago"
      },
      "to": {
        "type": "string",
        "format": "date",
        "description": "End date (YYYY-MM-DD). Defaults to today"
      }
    },
    "required": [
      "channelId"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}