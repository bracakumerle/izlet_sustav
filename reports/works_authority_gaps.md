# Works Authority Gaps

Scope: Top 10 authority-page candidates from `reports/works_authority_candidates.md`.

## 1. Žalo (`zalo`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: repository has MTV Adria rotation and HRT Radio Dubrovnik references; no direct media article ID specific to the song in `media_registry.json`.
- Missing schema properties: `duration`, `isrcCode`, `recordingOf`, direct `sameAs` for the recording.
- Missing cross-links: related work pages are not yet present for most peer songs.
- Missing authority assets: dedicated image/cover asset not identified in repository data.

## 2. Čast (`cast`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: `registry/media_registry.json` has ART011; `works_registry.json` has no media reference in the work node.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`, album relationship beyond registry text.
- Missing cross-links: no existing page before this generation.
- Missing authority assets: no dedicated cover/image asset identified in repository data.

## 3. U svijetu bajki (`u_svijetu_bajki`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: media registry entries are present; work node has no media references.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.

## 4. Zašto šutim (`zasto_sutim`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: media registry entry is present; work node has no media references.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.

## 5. Pernastica (`pernastica`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: no song-specific media article found in `media_registry.json`.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.

## 6. Čaroban otok (`caroban_otok`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: media registry entry is present; work node has no media references.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.

## 7. Kokosovo mlijeko (`kokosovo_mlijeko`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: media registry entry is present; work node has no media references.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.

## 8. Tko si ti (`tko_si_ti`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: media registry entry is present; work node has no media references.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.

## 9. Majčin skut (`majcin_skut`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: no song-specific media article found in `media_registry.json`.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.

## 10. Princeza Tea (`princeza_tea`)

- Missing identifiers: MusicBrainz work ID, MusicBrainz recording ID, Discogs master ID, Wikidata QID.
- Missing media references: no song-specific media article found in `media_registry.json`.
- Missing schema properties: `duration`, direct `sameAs`, `recordingOf`.
- Missing cross-links: no existing work page.
- Missing authority assets: no dedicated image/cover asset identified in repository data.
